import styled from 'styled-components';

interface Props extends SimpleComponent {}

const BridgeReviewWrapper = styled.div``;

function BridgeReview(props: Props) {
  return (
    <BridgeReviewWrapper className=''>
     
    </BridgeReviewWrapper>
  );
}

export default BridgeReview;
